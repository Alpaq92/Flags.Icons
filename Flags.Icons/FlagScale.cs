namespace Flags.Icons {
    /// <summary>
    /// PNG raster scale (FlagKit ships @1x, @2x and @3x variants). Always <see cref="X1"/> for SVG.
    /// </summary>
    public enum FlagScale {
        X1,
        X2,
        X3,
    }
}
