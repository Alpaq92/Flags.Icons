namespace Flags.Demo.Shared {
    /// <summary>
    /// What the demo's source-picker combobox selects: one of the 4 bundled sources or "All".
    /// </summary>
    public enum DemoSource {
        Twemoji,
        Circle,
        Square,
        Lipis,
        All,
    }
}
